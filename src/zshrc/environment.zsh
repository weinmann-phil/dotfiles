# This file holds the content of what is going to be the `.zshenv` file within 
# the home directory.

[ -f "$HOME/.cargo/env" ] && source "$HOME/.cargo/env"

# Themes

## Powerline
source /usr/share/powerline/bindings/bash/powerline.sh
